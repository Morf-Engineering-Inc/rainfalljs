# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# package-lock.json
yarn.lock

# Build outputs
dist/
lib/
coverage/
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor specific files
.idea/
.vscode/
*.sublime-project
*.sublime-workspace
.DS_Store
.directory
Thumbs.db
desktop.ini

# Logs
logs
*.log

# Testing
coverage/
.nyc_output/

# Temporary files
tmp/
temp/
# Local archives and scratch material. This repo is public: a zip swept in by a
# stray `git add` is republished to everyone and stays in history after deletion.
*.zip
*.tar.gz

# The pre-Data-Map prototype. Superseded by src/datamap/ — kept on disk as the
# source it was grown from, deliberately not published.
rainfall_generalize/
